# Spec Lock (Template)

Purpose:
- Freeze "keys / paths / precedence / allowed values / experimental flags" using official primary sources before design.

Metadata:
- Tool:
- Component (CLI / IDE / server / agent / plugin):
- Version (exact):
- Date (Asia/Tokyo):
- Scope (repo / machine / org):
- Owner:

1. Official primary sources (URLs)
- Docs:
- GitHub repo:
- Release notes:
- Issues (note-only, not spec):

2. Frozen facts (must be supported by the sources)
2.1 Keys (exact spelling)
- Key:
  - Purpose:
  - Allowed values:
  - Where it is set (file path):
  - Notes (experimental/preview):

2.2 File locations (exact paths)
- File/Dir:
  - Role:
  - Precedence (wins/loses against):

2.3 Precedence rules (override order)
- Highest → lowest:

3. Non-goals / out of scope
- 

4. Verification commands (WSL / CLI)
- Command:
  - Expected output:

5. Decisions enabled by this Spec Lock
- 

